window.CHATBOT_CONFIG = {
  // Point the front end to your deployed proxy server.
  apiBaseUrl: "https://your-deployment.example.com/api/chat",
  // Optionally override the model or temperature if your backend allows it.
  model: "gpt-4o-mini",
  temperature: 0.6,
};
